<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>

	<!-- Export and upload in one step, so there is no separate altool/Transporter
	     call to keep working. -->
	<key>destination</key>
	<string>upload</string>

	<key>teamID</key>
	<string>67F2439F4X</string>

	<!-- Manual, not automatic. Automatic signing makes Xcode ask App Store
	     Connect to mint a distribution profile through cloud signing, which an
	     API key is not permitted to do — it fails with "Cloud signing permission
	     error". Naming the profile explicitly avoids that path entirely. -->
	<key>signingStyle</key>
	<string>manual</string>
	<key>signingCertificate</key>
	<string>Apple Distribution</string>

	<!-- The workflow overwrites this value with the UUID of the profile it just
	     installed, so the profile can be renamed without touching this file. -->
	<key>provisioningProfiles</key>
	<dict>
		<key>com.henrydashwood.hearful</key>
		<string>REPLACED_BY_WORKFLOW</string>
	</dict>

	<!-- The workflow sets CURRENT_PROJECT_VERSION from the commit count; Xcode
	     must not overwrite it. -->
	<key>manageAppVersionAndBuildNumber</key>
	<false/>

	<key>uploadSymbols</key>
	<true/>
</dict>
</plist>
