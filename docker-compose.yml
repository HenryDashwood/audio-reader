services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: audioreader
      POSTGRES_PASSWORD: audioreader
      POSTGRES_DB: audioreader
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
